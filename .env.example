OPENROUTER_API_KEY=
EXA_API_KEY=
TRIGGER_SECRET_KEY=
AUTH0_SECRET=
AUTH0_BASE_URL=http://localhost:3000
AUTH0_ISSUER_BASE_URL=
AUTH0_CLIENT_ID=
AUTH0_CLIENT_SECRET=
TRIGGER_PROJECT_REF=
APP_URL=http://localhost:3000
# LLM_CHEAP_MODEL=openai/gpt-4o-mini
# LLM_STRONG_MODEL=openai/gpt-4o
# DEMO_FAIL_ONCE=news   # makes the first Exa call fail so the retry shows in the activity feed

# Storage: leave empty for local JSON files under ./data. Set both to use Upstash Redis (required on Vercel / Cloud Run).
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
